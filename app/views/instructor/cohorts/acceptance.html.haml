%table.table
  %thead
    %tr
      %th(rowspan="2") Name
      %th(colspan="3" class="text-center") gCamp
    %tr
      %th Github
      %th gCamp Tracker
      %th gCamp URL
  %tbody
    - @users.each do |user|
      %tr
        %td= user.full_name
        %td= link_to "#{user.github_username}/gCamp", "http://github.com/#{user.github_username}/gCamp", target: :_blank
        %td= link_to user.gcamp_tracker_url, user.gcamp_tracker_url
        %td= link_to user.gcamp_url, user.gcamp_url
