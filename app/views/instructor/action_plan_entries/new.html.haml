.page-header
  %h1 #{@user.full_name}: New Action Plan Entries

= render partial: 'instructor/cohorts/nav', locals: { cohort: @cohort }

= form_for [:instructor, :cohort, :student, @entry] do |f|
  = render 'form', f: f
