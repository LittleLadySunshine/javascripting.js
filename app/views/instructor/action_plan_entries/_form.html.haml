= errors_for(f)

.form-group
  = f.label :description
  = f.text_area :description, class: "form-control", rows: 20, autofocus: true

.form-group
  .alert.alert-warning NOTE: this will send an email to the student with the contents of the plan
  = f.submit class: 'btn btn-primary'
