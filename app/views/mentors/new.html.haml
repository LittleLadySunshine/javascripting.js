.page-header
  %h1 New Mentor

- if user_session.current_user.instructor?
  = render 'instructor/cohorts/nav', cohort: @cohort
- else
  = render 'student/sub_nav'

= form_for @mentor, url: cohort_student_mentors_path(@cohort, @user), html: {class: "form-horizontal"} do |f|
  = render 'form', f: f
